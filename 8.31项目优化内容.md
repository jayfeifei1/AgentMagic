# 8.31 项目优化内容

本次迭代围绕“可演示、可追踪、可落库”的目标，对原智能客服项目进行了二次开发。

## 1. 架构与部署

- 前端移除 Java 后端连接配置，只保留 Python FastAPI 服务。
- 使用 Docker Compose 部署前端、Python 后端、Redis、MySQL、ChromaDB、Nginx 和 Prometheus。
- 补充 Docker 部署说明，记录 MySQL、Redis 的可视化连接方式。

## 2. Trace 可观测性一期

- 将原先只保存在内存中的 Trace 持久化到 MySQL。
- 建立 `request_traces`、`agent_execution_traces`、`llm_call_traces`、`tool_call_traces` 四张表，记录请求路由、Agent 最终输出、工具调用、模型调用、耗时、状态与异常信息。
- `request_traces.final_response` 保存最终呈现给用户的答复；多 Agent 场景额外保存 `composer_content`，便于对比合并前后结果。
- 前端支持查看请求 Trace；支持按请求、时间、Agent、工具定位链路。
- MySQL 时间统一按本地时区写入，方便排障核对。

## 3. 对话与前端体验

- 补充 AI 回复加载状态，避免用户误以为页面卡顿。
- 修复刷新页面后会话内容丢失的问题，结合会话记忆恢复历史对话。
- 修复 Chroma 会话记忆过滤条件，保证不同会话的历史记录隔离。

## 4. MySQL 真实业务工具

将原有“模拟返回”的订单、账单、退款和技术排障工具改为受控的真实 MySQL 查询，模型只能通过 Function Calling 调用预定义工具，不能生成任意 SQL。

- 通用 Agent：订单状态查询。
- 账单 Agent：支付流水汇总、重复扣款核验、退款进度查询。
- 技术 Agent：错误码手册、服务故障状态查询。
- 升级 Agent：创建真实人工交接工单。

新增业务表：`orders`、`payment_transactions`、`refunds`、`error_code_playbooks`、`service_incidents`、`handoff_tickets`。

初始化了多用户演示数据，覆盖订单支付、支付失败、重复扣款、退款处理中、退款完成、配送中、401/500/502 服务故障等场景。

## 5. 转人工策略

- 修复“回复里出现转人工字样就误升级”的问题；现在只有 Agent 明确调用 `request_human_handoff` 工具才会标记升级。
- 删除基于“紧急、立刻、asap”等关键词的自动升级逻辑，避免普通订单查询被误转人工。
- 当前仅在用户明确要求人工、投诉升级，或领域 Agent 判断需要人工介入并创建工单时转人工。
- 保留故障表中的 `CRITICAL` 严重级别，用于描述技术事故，不等同于用户消息的转人工条件。

## 6. 验证

- 修复模型工具调用后偶发空文本被误标记成功的问题：空文本会触发一次无工具的最终答复补偿调用，仍为空则明确记为失败。
- 统一提高 Agent 与 Composer 的生成预算，并关闭 DeepSeek 默认思考模式，减少可见答复被截断的概率。
- Agent 编排测试与会话记忆测试均已回归通过。
- 已用真实接口验证订单、退款、支付故障、502 事故查询和人工工单创建链路。
