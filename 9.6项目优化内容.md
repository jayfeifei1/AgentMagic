# 9.6 项目优化内容

本次迭代聚焦 RAG 知识库链路，完成了知识文档解耦、多格式解析、结构化切片、本地向量化、检索质量优化和容器环境稳定性修复。

## 1. 默认知识文档解耦

- 将原先写在代码中的 6 篇简单知识迁移到 `data/knowledge/seed`，由知识库启动时自动加载。
- 重新扩充订单配送、退款售后、支付账单、账户安全、技术排障、会员订阅六类业务文档，使知识内容与 GeneralAgent、BillingAgent、TechnicalAgent 的职责和工具场景对应。
- 使用独立的 `knowledge_base_bge_v1` Collection 保存 RAG 数据，不修改情景记忆、用户画像和意图识别使用的数据与逻辑。

## 2. 多格式文档上传与解析

- 知识库上传接口支持 TXT、Markdown、JSON、PDF、DOCX 以及常见图片格式。
- TXT、Markdown、JSON 在后端直接解码为文本；PDF、DOCX 和图片调用 MinerU 轻量 API，统一解析为 Markdown。
- 上传的原始文件保存到 `data/knowledge/raw/<document_id>/`，解析后的 Markdown 保存到 `data/knowledge/parsed/`，通过唯一 `document_id` 避免重名文件互相覆盖。
- MinerU 调用链覆盖任务创建、文件上传、状态轮询、结果下载、超时和明确错误返回；扫描版 PDF 与图片可通过 MinerU OCR 识别。
- 针对 Windows Docker 容器下载 MinerU CDN 文件时出现 TLS EOF 的问题，增加宿主机 MinerU Relay。容器通过 `host.docker.internal` 获取解析结果，Relay 配置已写入 Docker Compose 和部署指南，服务重启后可按固定流程恢复。

## 3. 结构化 Token 切片

- 替换原有固定 500 字符切片，改为 Markdown 标题层级感知切片。
- 先按标题形成 Section，并为每个切片保留完整标题路径；子级内容可继承父级短正文，避免切片脱离业务语境。
- 以 384 Tokens 为目标、480 Tokens 为硬上限；Section 超长时再按段落和句子递归拆分。
- 仅对超长内容加入 48 Tokens 左右的重叠，减少边界处语义断裂，普通短 Section 不制造重复内容。
- Markdown 表格和 HTML 表格作为独立内容处理；长表格按行拆分并重复表头，极端超长单行也会继续安全切分。
- 每个切片保存 `document_id`、源文件、标题路径、Section 层级、内容类型、Token 数、切片序号、`content_hash` 和 `chunker_version` 等元数据。

## 4. 本地 BGE 向量化

- RAG 不再依赖 ChromaDB 默认的 `all-MiniLM-L6-v2`，统一调用本地部署的 `BAAI/bge-small-zh-v1.5` Embedding 服务。
- BGE 服务同时提供向量生成和 Token 计数，保证切片长度与实际向量模型的分词方式一致。
- 使用 512 维向量写入本地 ChromaDB，并重建为干净的 `knowledge_base_bge_v1` Collection。

## 5. 检索链路优化

- Agent 按需调用知识库工具后，依次执行原始问题改写、多子查询并行召回、结果合并去重、LLM 重排和 Top-K 返回。
- 多路召回按照稳定的文档与切片标识去重，不再因不同子查询产生的分数不同而保留重复内容，并优先保留最高相似度结果。
- 限制进入重排阶段的候选数量，减少无效上下文和模型调用开销。
- 重排结果支持纯 JSON、Markdown 代码块和带说明文本的 JSON；校验索引合法性、唯一性，并在返回数量不足时按向量顺序补齐。
- LLM 重排失败时保留原向量顺序并正确返回 `reranked=false`，避免把降级结果误标记为重排成功。
- 调整查询改写与重排的生成预算，解决推理模型因输出预算不足而只生成思考内容、没有最终 JSON 的问题。

## 6. 缓存与知识同步

- 为“原始问题 → 最终 Top-K”完整检索结果增加 TTL 缓存，命中后跳过重复的查询改写、向量召回和 LLM 重排。
- 新增或上传知识文档后主动清理知识检索缓存，避免继续返回旧结果。
- 使用 SHA-256 `content_hash` 检测默认文档和上传文档的内容变化；文件修改后自动删除旧切片并重新入库，文件移除后清理对应失效切片。
- 解析验证文档已移至 `data/knowledge/parsed/test`，不会进入正式知识库；当前正式 RAG Collection 只保留六篇默认文档生成的 30 个切片。

## 7. 稳定性修复

- 修复 ChromaDB 0.5.23 与新版 PostHog 的 telemetry 调用签名不兼容问题，将依赖约束为 `posthog<6`。
- Docker 镜像重新构建后，后端、BGE、ChromaDB、Redis、MySQL 和 MinerU Relay 均可按部署指南启动。
- 容器重启后知识库数据仍保持 30 个正式切片，情景记忆和用户画像 Collection 数据未受影响。

## 8. 回归验证结果

- 真实解析链路已验证文本型 PDF、扫描型 PDF OCR 和 DOCX，均可通过 MinerU 解析并将 Markdown 落到本地。
- 知识库切片、内容同步、上传接口、查询改写、召回去重、重排降级和完整链路缓存测试通过。
- Agent 编排与会话记忆原有测试同步回归，共计 40 项聚焦测试通过。
- 使用 12 条人工标注问题测试正式知识库的纯向量召回：`Hit@1 = 91.7%`，`Hit@3 = 100%`，无 Top-3 漏召回。
- 本地 BGE + ChromaDB 纯向量检索中位耗时约 `16.99 ms`，P95 约 `20.49 ms`。
- 包含查询改写和 LLM 重排的首次完整检索约 `3.4～9.0 s`；相同问题命中完整链路缓存后约 `3.4～3.6 ms`。

